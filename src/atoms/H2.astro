---
const {

    right,
    leading,
    center,
    light

} = Astro.props
---
<h2 class={` ${ center ? "text-center" : "" } ${ light ? "text-white" : "" } ${ right ? "md:text-right" : "md:text-left" } ${ leading ? "md:leading-snug" : "md:leading-[56px]" } font-trumpsoftpro text-[44px] md:text-[64px] text-urbaninsight_black uppercase`}>
    <slot />
</h2>
